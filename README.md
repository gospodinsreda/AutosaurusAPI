# BotasaurusAPI 🤖

**Мощный API для автоматизации браузера на основе Botasaurus + FastAPI**

Управляйте сессиями браузера, выполняйте действия, запускайте скрипты автоматизации, парсите страницы с BeautifulSoup и управляйте браузером с помощью AI — всё через единый REST API и веб-панель на русском языке.

---

## 🚀 Возможности

- **Управление сессиями** — Создание, настройка и управление множеством браузерных сессий с прокси, headless режимом, обходом Cloudflare, профилями, человеческим режимом и многим другим
- **60+ действий браузера** — Навигация, клики, ввод текста, скроллинг, извлечение данных, выполнение JavaScript, управление cookies, скриншоты, работа с iframe и вкладками
- **Движок скриптов** — JSON-based язык скриптов с переменными, условиями, циклами, обработкой ошибок и логированием
- **Парсер Soupify** — Встроенный парсинг с BeautifulSoup с правилами структурированного извлечения, парсинг ссылок/изображений/таблиц/мета-тегов
- **Менеджер Cookie** — Загрузка и экспорт cookies в формате Netscape
- **AI Агент** — Управление браузером через естественный язык с помощью LLM (LM Studio, OpenAI-совместимые API)
- **Веб-панель** — Красивый SPA интерфейс на русском языке для управления сессиями, редактирования скриптов, выполнения действий и AI управления
- **Полная документация API** — Автоматически генерируемая документация Swagger UI и ReDoc

---

## 📦 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Запуск сервера

```bash
python main.py
```

Или с помощью uvicorn напрямую:

```bash
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

### 3. Откройте

- **Веб-панель**: http://localhost:8000
- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

---

## 🔌 API Endpoints

### Сессии

| Метод | Endpoint | Описание |
|--------|----------|-------------|
| POST | `/api/sessions` | Создать новую браузерную сессию |
| GET | `/api/sessions` | Список всех сессий |
| GET | `/api/sessions/{id}` | Получить детали сессии |
| DELETE | `/api/sessions/{id}` | Закрыть сессию |
| DELETE | `/api/sessions/{id}/remove` | Закрыть и удалить сессию |
| POST | `/api/sessions/close-all` | Закрыть все сессии |

### Действия

| Метод | Endpoint | Описание |
|--------|----------|-------------|
| POST | `/api/actions/{session_id}` | Выполнить одно действие |
| POST | `/api/actions/{session_id}/batch` | Выполнить пакет действий |

### Скрипты

| Метод | Endpoint | Описание |
|--------|----------|-------------|
| POST | `/api/scripts` | Создать скрипт |
| GET | `/api/scripts` | Список всех скриптов |
| GET | `/api/scripts/{id}` | Получить скрипт |
| PUT | `/api/scripts/{id}` | Обновить скрипт |
| DELETE | `/api/scripts/{id}` | Удалить скрипт |
| POST | `/api/scripts/run` | Запустить скрипт |

### Парсинг

| Метод | Endpoint | Описание |
|--------|----------|-------------|
| POST | `/api/parse/soupify` | Умный парсинг страницы (ссылки, изображения, таблицы, мета) |
| POST | `/api/parse/extract` | Структурированное извлечение данных с правилами |
| POST | `/api/parse/css` | Быстрый CSS селектор запрос |

### AI Агент

| Метод | Endpoint | Описание |
|--------|----------|-------------|
| GET | `/api/ai/config` | Получить конфигурацию AI |
| PUT | `/api/ai/config` | Обновить конфигурацию AI |
| POST | `/api/ai/models` | Получить список моделей от LM Studio |
| POST | `/api/ai/run` | Запустить AI задачу автоматизации браузера |
| GET | `/api/ai/history/{session_id}` | Получить историю разговоров |

---

## ⚙️ Конфигурация сессии

```json
{
  "config": {
    "proxy": "http://user:pass@host:port",
    "headless": false,
    "bypass_cloudflare": true,
    "block_images": true,
    "block_images_and_css": false,
    "user_agent": "random",
    "window_size": "1920x1080",
    "profile": "my-profile",
    "tiny_profile": true,
    "lang": "ru",
    "human_mode": true,
    "cookies_file": "cookies.txt",
    "add_arguments": ["--disable-gpu"],
    "timeout": 30
  }
}
```

---

## 📝 Язык скриптов

BotasaurusAPI использует JSON-based язык скриптов с полным контролем потока выполнения:

```json
{
  "name": "Мой скрапер",
  "variables": { "base_url": "https://example.com" },
  "session_config": { "headless": true },
  "steps": [
    { "action": "navigate", "url": "$base_url" },
    { "action": "get_title", "save_as": "title" },
    {
      "action": "if",
      "condition": { 
        "variable": "$title", 
        "operator": "contains", 
        "value": "Example" 
      },
      "then_steps": [
        { "action": "soupify_select_all", "selector": "a", "save_as": "links" }
      ]
    },
    {
      "action": "loop",
      "loop_range": [1, 5],
      "loop_variable": "page",
      "loop_steps": [
        { "action": "navigate", "url": "$base_url?page=$page" },
        { "action": "get_page_html", "save_as": "html_$page" }
      ]
    }
  ]
}
```

### Поддерживаемые действия (60+)

**Навигация**: `navigate`, `google_get`, `get_via`, `back`, `forward`, `refresh`

**Взаимодействие**: `click`, `type`, `clear`, `select_option`, `scroll`, `scroll_into_view`, `hover`, `focus`, `drag_and_drop`

**Извлечение**: `get_text`, `get_attribute`, `get_html`, `get_page_html`, `get_page_text`, `get_url`, `get_title`, `get_cookies`, `is_element_present`

**Парсинг**: `soupify`, `soupify_select`, `soupify_select_all`

**JavaScript**: `run_js`, `run_cdp`

**Cookies**: `set_cookies`, `delete_cookies`, `load_cookies_netscape`

**Скриншоты**: `screenshot`, `screenshot_element`

**Вкладки**: `new_tab`, `switch_tab`, `close_tab`, `get_tabs`

**iFrames**: `select_iframe`, `get_iframe_by_link`, `exit_iframe`

**Ожидание**: `sleep`, `random_sleep`, `wait_for_navigation`, `wait_for_element`

**Человеческий режим**: `enable_human_mode`, `disable_human_mode`

**Fetch запросы**: `fetch_get`, `fetch_post`

**Управление потоком**: `if` (условие), `loop` (цикл)

### Интерполяция переменных

Используйте `$variable_name` в любом строковом поле. Сохраняйте результаты с помощью `"save_as": "my_var"`.

### Обработка ошибок

Каждый шаг поддерживает `"on_error"`: `"skip"` | `"retry"` | `"abort"` с опциональным `"max_retry"`.

---

## 🤖 AI Модуль

AI модуль подключается к любому OpenAI-совместимому API (LM Studio, Ollama и т.д.):

### 1. Настройка через веб-интерфейс

1. Откройте веб-панель http://localhost:8000
2. Перейдите на вкладку "Настройки"
3. Введите endpoint LM Studio (например: `http://localhost:1234/v1`)
4. Нажмите "Загрузить модели" для получения списка доступных моделей
5. Выберите модель из выпадающего списка
6. Сохраните настройки

### 2. Настройка через API

```bash
curl -X PUT http://localhost:8000/api/ai/config \
  -H "Content-Type: application/json" \
  -d '{
    "endpoint": "http://localhost:1234/v1",
    "model": "local-model",
    "enabled": true
  }'
```

### 3. Запуск AI задачи

```bash
curl -X POST http://localhost:8000/api/ai/run \
  -H "Content-Type: application/json" \
  -d '{
    "session_id": "abc123",
    "goal": "Перейти на google.com, найти информацию о web scraping, извлечь первые 5 заголовков результатов",
    "max_steps": 15
  }'
```

AI агент будет наблюдать за страницей, планировать действия, выполнять их шаг за шагом и сообщать результаты.

### Сохранение истории

Все диалоги с AI автоматически сохраняются в базу данных и доступны через:
- Веб-интерфейс (вкладка "AI Агент")
- API endpoint `/api/ai/history/{session_id}`

---

## 🍪 Загрузка Cookie (формат Netscape)

Загрузите cookies из файла формата Netscape:

```bash
curl -X POST http://localhost:8000/api/actions/{session_id} \
  -H "Content-Type: application/json" \
  -d '{
    "action": "load_cookies_netscape",
    "cookies_text": "# Netscape HTTP Cookie File\n.example.com\tTRUE\t/\tFALSE\t0\tsession_id\tabc123"
  }'
```

Или укажите файл cookies при создании сессии:

```json
{ "config": { "cookies_file": "my_cookies.txt" } }
```

---

## 📁 Структура проекта

```
BotasaurusAPI/
├── main.py                    # Точка входа FastAPI
├── requirements.txt           # Зависимости
├── app/
│   ├── config.py              # Конфигурация
│   ├── core/
│   │   ├── browser_engine.py  # Обертка Botasaurus Driver
│   │   ├── session_manager.py # Управление жизненным циклом сессий
│   │   ├── action_executor.py # Движок выполнения действий
│   │   ├── script_engine.py   # Парсер и исполнитель скриптов
│   │   └── cookie_manager.py  # Обработка Netscape cookies
│   ├── models/
│   │   ├── session.py         # Модели сессий
│   │   ├── action.py          # Модели действий (60+ типов)
│   │   ├── script.py          # Модели скриптов
│   │   └── response.py        # Модели ответов
│   ├── api/
│   │   ├── sessions.py        # Эндпоинты сессий
│   │   ├── actions.py         # Эндпоинты действий
│   │   ├── scripts.py         # Эндпоинты скриптов
│   │   ├── parsing.py         # Эндпоинты парсинга
│   │   └── ai.py              # Эндпоинты AI агента
│   ├── ai/
│   │   ├── llm_client.py      # OpenAI-совместимый LLM клиент
│   │   ├── agent.py           # AI браузер-агент
│   │   ├── prompts.py         # Системные промпты (на русском)
│   │   └── config.py          # Хранение конфигурации и истории AI
│   └── web/
│       └── static/
│           └── index.html     # Веб-панель (SPA на русском)
├── data/
│   ├── sessions/              # Данные сессий
│   ├── scripts/               # Сохраненные скрипты
│   ├── cookies/               # Файлы cookies
│   └── autosaurus.db          # База данных SQLite
└── scripts/
    └── example_google_search.json  # Примеры скриптов
```

---

## 💾 Сохранение данных

Все важные данные автоматически сохраняются в SQLite базу данных:

- **Скрипты** — Все созданные скрипты сохраняются с метаданными
- **Конфигурация AI** — Endpoint, модель, API ключ
- **История разговоров** — Все диалоги с AI агентом
- **Настройки** — Пользовательские настройки приложения

База данных находится по пути: `data/autosaurus.db`

---

## 🎯 Особенности реализации

### Максимальная функциональность

- ✅ Персистентное хранилище для всех данных
- ✅ Интеграция с LM Studio для получения списка моделей
- ✅ История всех AI диалогов с привязкой к сессиям
- ✅ Гибкая система конфигурации
- ✅ Автоматическая очистка устаревших сессий
- ✅ Подробное логирование всех операций
- ✅ Обработка ошибок с retry логикой
- ✅ Веб-интерфейс полностью на русском языке

### Веб-интерфейс

Современный SPA интерфейс с:
- 📊 Панель управления сессиями
- ⚡ Выполнение действий с динамическими формами
- 📝 JSON редактор для скриптов
- 🤖 AI агент с историей разговоров
- ⚙️ Настройки с выпадающим списком моделей от LM Studio
- 🎨 Адаптивный дизайн на Tailwind CSS
- 🌐 Полностью на русском языке

---

## 📖 Примеры использования

### Создание сессии

```python
import requests

response = requests.post('http://localhost:8000/api/sessions', json={
    "config": {
        "headless": False,
        "lang": "ru",
        "human_mode": True
    },
    "name": "Моя тестовая сессия"
})

session = response.json()
session_id = session['session']['session_id']
```

### Выполнение действий

```python
# Навигация
requests.post(f'http://localhost:8000/api/actions/{session_id}', json={
    "action": "navigate",
    "url": "https://example.com"
})

# Клик
requests.post(f'http://localhost:8000/api/actions/{session_id}', json={
    "action": "click",
    "selector": "button.login"
})

# Извлечение текста
response = requests.post(f'http://localhost:8000/api/actions/{session_id}', json={
    "action": "get_text",
    "selector": "h1"
})
print(response.json()['result'])
```

### Запуск скрипта

```python
response = requests.post('http://localhost:8000/api/scripts/run', json={
    "script_id": "script-123",
    "variables": {
        "search_query": "python automation"
    }
})
```

### Использование AI агента

```python
response = requests.post('http://localhost:8000/api/ai/run', json={
    "session_id": session_id,
    "goal": "Найти на Wikipedia информацию о Python и сохранить краткое описание",
    "max_steps": 10
})

print(response.json()['transcript'])
```

---

## 🔧 Системные требования

- Python 3.8+
- Chrome или Chromium браузер
- 4GB+ RAM
- LM Studio (опционально, для AI функций)

---

## 📄 Лицензия

MIT License

---

## 🤝 Поддержка

Для вопросов и обсуждения используйте Issues на GitHub.

---

## 🌟 Особенности

Этот проект создан с учетом лучших практик из существующих решений:
- Browser automation frameworks (Selenium, Playwright, Puppeteer)
- LM Studio integration patterns
- FastAPI best practices
- Modern SPA design patterns

**Результат**: Максимально функциональный и гибкий API для автоматизации браузера с AI управлением! 🚀
